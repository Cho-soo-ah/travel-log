# 📂 프로젝트 폴더 구조
```
TRAVLE-LOG
├── app/                  # (Expo Router 사용 시) 화면 라우팅 및 엔트리
│   ├── index.tsx         # 메인 여행 리스트 화면
│   ├── (trip)/           # 여행 상세 관련 그룹
│   │   ├── [id].tsx      # 여행 상세(요약) 화면
│   │   └── day-[num].tsx # Day 상세(동선+지출) 화면
│   └── translate.tsx     # 이미지 번역 화면
├── components/           # 재사용 UI 컴포넌트
│   ├── Day/
│   │   ├── SpotItem.tsx      # 리스트 아이템 컴포넌트
│   │   ├── RouteConnector.tsx # 아이콘 사이 연결선
│   │   └── DayHeader.tsx     # 숙소 토글 및 헤더
│   ├── Map/
│   │   └── TravelMap.tsx     # 지도 및 폴리라인 구현
│   └── Common/
│       ├── WeatherBadge.tsx
│       └── ExchangeWidget.tsx
├── hooks/                # 커스텀 훅
│   ├── useTravel.ts      # 여행 데이터 관리 로직
│   └── useExchange.ts    # 환율 계산 로직
├── types/                # TypeScript 정의
│   ├── index.ts          # 전체 타입 export
│   └── travel.d.ts       # 여행/장소/지출 인터페이스
├── utils/                # 유틸리티 함수
│   ├── format.ts         # 통화, 날짜 포맷팅
│   └── mapHelper.ts      # 좌표 계산 등 지도 관련 함수
├── constants/            # 상수 및 설정
│   └── colors.ts         # 앱 테마 색상
├── App.tsx               # (Expo Router 미사용 시) 메인 엔트리
├── app.json
└── package.json
```